---
import Button from '@components/Button.astro';

export interface CardInfoProps {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`services-card ${className}`}>
  <slot />
  <Button>Read More</Button>
</div>
